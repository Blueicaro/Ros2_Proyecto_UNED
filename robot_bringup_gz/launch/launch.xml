<launch>
    <!-- Ruta al paquete que descripción del robot-->
    <let name="pkg_share" value="$(find-pkg-share robot_description)" />

    <!-- Ruta al archivo de configuración de Gazebo Bridge -->
    <let name="gazebo_config_path"
        value="$(find-pkg-share robot_bringup_gz)/config/gazebo_bridge.yaml" />


    <!-- Se debe indicar a Gazebo dónde está las texturas-->
    <set_env name="GZ_SIM_RESOURCE_PATH" value="$(var pkg_share)/.." />

    <!-- Ruta al archivo URDF-->
    <let name="urdf_path" value="$(find-pkg-share robot_description)/urdf/robot_final.urdf.xacro" />

    <node pkg="robot_state_publisher" exec="robot_state_publisher">
        <param name="robot_description" value="$(command 'xacro $(var urdf_path)')" />
    </node>


    <include
        file="$(find-pkg-share ros_gz_sim)/launch/gz_sim.launch.py">
        <arg name="gz_args" value="empty.sdf -r" />
    </include>
    <!-- Lanzar gazebo -->
    <node pkg="ros_gz_sim" exec="create" args="-topic robot_description -name my_robot -z 0.5" />

    <!-- Lanzar bridge -->
     <node pkg="ros_gz_bridge" exec="parameter_bridge">
        <param name="config_file" value="$(var gazebo_config_path)" />        
     </node> 
</launch>